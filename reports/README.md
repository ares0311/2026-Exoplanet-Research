# Reports

Background search report exports are written under:

```text
reports/background/
```

Generated Markdown and HTML reports are runtime review artifacts and are ignored by default. Promote a report into version control only through an explicit review decision.
