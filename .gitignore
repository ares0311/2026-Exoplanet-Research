# Claude Code internal state
.claude/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg
MANIFEST

# Virtual environments
.venv/
.venv*/
venv/
venv*/
env/
.env

# Testing
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Type checkers
.mypy_cache/
.dmypy.json
dmypy.json

# Linters
.ruff_cache/

# Exact-revision external representation weights and Hugging Face local-dir metadata
.cache/representation_models/

# Hash-pinned shared Catalina catalog cache used by the 6x6 crossmatch pilot
.cache/stellar_variability_labels/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data files (raw downloads — committed intentionally via DVC or similar)
data/raw/
data/processed/
data/*.csv
data/*.pkl
data/*.pickle
data/*.json
data/*.jsonl
data/*.sqlite
data/*.sqlite3
data/*_splits/
data/new_tess_targets.txt
data/.xdg_cache_sandbox/

# Download checkpoints (local resume state — never committed)
checkpoints/

# Local CNN training experiments and rejected checkpoints.
# Production-approved checkpoints are committed only after gate approval.
models/cnn/
models/cnn_*/
models/cnn*/

# This repository does not currently use a committed uv lockfile.
uv.lock

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Misc generated application artifacts
.alcache/
.alpackages/
.snapshots/
.output/
*.app
rad.json
*.g.xlf
*.bclicense
*.flf
TestResults.xml

# Cross-process fcntl lock sidecar files (watchlist.py, candidate_timeline.py,
# crossmatch_tess_catalina_labels.py) -- operational artifacts, never data.
*.json.lock

# Runtime automation state. Keep only committed documentation/ignore sentinels.
logs/*
!logs/
!logs/.gitignore
!logs/README.md

# Runtime report exports. Keep only committed documentation/ignore sentinels.
reports/*
!reports/
!reports/README.md
!reports/background/
reports/background/*
!reports/background/.gitignore
